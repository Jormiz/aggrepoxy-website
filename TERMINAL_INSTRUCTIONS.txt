AGGREPOXY WEBSITE UPDATE — TERMINAL INSTRUCTIONS
==================================================

Do this in Terminal — do NOT drag files in Finder.

--------------------------------------------------
STEP 1 — Go to your local project folder
--------------------------------------------------
cd ~/Desktop/aggrepoxy-website

--------------------------------------------------
STEP 2 — Copy the files in (replace SOURCE_FOLDER with wherever you
unzipped this delivery, e.g. ~/Downloads/delivery3)
--------------------------------------------------
cp SOURCE_FOLDER/index.html ./index.html
cp SOURCE_FOLDER/la.html ./la.html
cp SOURCE_FOLDER/css/main.css ./css/main.css
cp SOURCE_FOLDER/css/home.css ./css/home.css
cp SOURCE_FOLDER/js/core.js ./js/core.js
cp SOURCE_FOLDER/js/home.js ./js/home.js

--------------------------------------------------
STEP 3 — Verify everything landed correctly
--------------------------------------------------
ls -la css/
ls -la js/

You should see main.css and home.css in css/ with today's timestamp,
and core.js and home.js in js/ with today's timestamp.

--------------------------------------------------
STEP 4 — Commit and push
--------------------------------------------------
git add -A
git status
   (confirm: index.html, la.html, css/main.css, css/home.css,
   js/core.js, js/home.js all modified)
git commit -m "Restyle hero eyebrows, garage thumbnail crop, quote banner, before/after section"
git push

==================================================
WHAT CHANGED — SUMMARY
==================================================

HERO (index.html)
- Eyebrow pills restyled: white text, cyan dot bullet, cyan outline,
  30% black fill — matching your reference image
- Text updated: "Los Angeles" → "Los Angeles & Orange County",
  "Chicago" → "Chicago Metropolitan Area"

TRANSFORM SECTION (index.html + la.html)
- Garage Floors thumbnail (Libertyville-Hero.jpg) now zoomed in and
  shifted down to show more floor and less of the garage door/ceiling
  — only this one thumbnail was touched, the other two cards are
  unchanged
- The "Get A Free Quote For Your Next Project" cyan bar at the bottom
  of the Why Choose Aggrepoxy section now sits flush against the very
  bottom edge of that section — no gap/padding below it, so the white
  "Concrete Transformations" section starts immediately after it, as
  in your reference image

BEFORE/AFTER SECTION — "Concrete Transformations" (index.html + la.html)
- Reordered: Dealership Showroom (your updated photo) is now the 2nd
  pair, Industrial/Locker Room moved to 4th
- Picked up your replacement dealer-after image (the extension
  changed from .jpg to .jpeg on your end — updated the reference so
  it still loads)
- Drag-handle icon replaced with a bigger, cleaner double-chevron
  (no more connecting bar running through the middle) and the handle
  circle itself is larger
- "Before"/"After" badges enlarged, and the "After" badge is now the
  actual brand cyan instead of the washed-out teal it was using
- This handle/badge styling is shared with the before/after widgets
  on the service pages (Garage Floors, Industrial Coatings, Concrete
  Polishing), so those got the bigger arrows and badges too, for
  consistency — their photos and order were not touched
